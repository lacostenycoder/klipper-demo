<% alert('hi'); %>
<% for(var i=0; i < 10; i++) { %>
  <% if(i % 3 == 0) { %>
    <div class="pure-g">
  <% } %>
  <div class="pure-u-1 pure-u-md-1-3" data-index="{{id}}">
    <a href="#" class="product-photo"><img src="{{image.src}}" height="130" alt="{{name.txt}}"/></a>
    <h2><a href="{{link.href}}"> {{name.text}} </a></h2>
    <div class="product-description">
      <div>Price: ${{price.text}}</div>
      <button>Buy Now!</button>
    </div>
    <%= if(i % 3 == 0) { %>
      </div>
    <% } %>
<% } %>
